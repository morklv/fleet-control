services:
  backend:
    build: ./backend
    environment:
      FLEET_CONTROL_DB: /data/fleet-control.db
    volumes:
      - fleet-data:/data
    ports:
      - "8100:8100"

  dashboard:
    build: ./frontend
    depends_on:
      - backend
    ports:
      - "8080:80"

volumes:
  fleet-data:
